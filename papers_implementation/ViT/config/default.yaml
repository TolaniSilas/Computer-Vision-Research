# ViT default config (an image is worth 16x16 words)
# override via CLI or experiment configs.


training:
  num_epochs: 20
  lr: 1e-4
  weight_decay: 0.0
  t_max: 5
  eta_min: 1e-6
  sanity_check: false
  path2weights: ./models/vision-transformer_pretrained.pt

data:
  batch_size: 32